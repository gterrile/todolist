export default class Event {
  constructor(id, title, description, due, priority, project) {
    this.id = id,
    this.title = title,
    this.description = description,
    this.due = due,
    this.priority = priority,
    this.project = project
  }
}

